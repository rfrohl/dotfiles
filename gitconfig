[core]
    excludesfile = ~/.gitignore
    editor = vim -c 'set spell nocursorline'

[user]
    email = frohl.robert@gmail.com
    name = rfrohl

[rerere]
    enabled = true

[color]
    ui = auto

[alias]
    a = add
    c = commit
    ca = commit -a
    d = diff
    l = log --topo-order --decorate
    p = pull
    sh = show
    st = status
    su = submodule update --init

[diff]
    rename = copy
    tool = vimdiff

[push]
    default = simple

[pull]
	ff = only
