[core]
    excludesfile = ~/.gitignore
    editor = vim -c 'set spell nocursorline'

[user]
    email = robert.frohl@posteo.de
    name = rfrohl
    signingKey = 0xCECDC85F79645A9E

[rerere]
    enabled = true

[color]
    ui = auto

[alias]
    a = add
    c = commit
    ca = commit -a
    d = diff
    l = log --topo-order --decorate
    p = pull
    sh = show
    st = status
    su = submodule update --init

[diff]
    rename = copy
    tool = vimdiff

[push]
    default = simple

[pull]
	ff = only

[gpg]
    program = gpg2

[commit]
    gpgsign = true
